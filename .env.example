# Copy to .env and set your values

# Base URL of the site (used for og:image, og:url, and canonical links)
# On Netlify: set to https://tricolorscreen.se in production, leave empty on deploy previews (falls back to DEPLOY_PRIME_URL)
# For local development: http://localhost:3000
SITE_URL=http://localhost:3000
